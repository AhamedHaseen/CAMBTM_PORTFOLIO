/*
 * Stands in for the `@cambt/ui` package the pages import Breadcrumb from. Alias `@cambt/ui` to this
 * file (tsconfig `paths`), or change the two imports to point here.
 */
export { Breadcrumb } from "./components/breadcrumb";
